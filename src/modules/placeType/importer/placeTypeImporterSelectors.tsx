import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const placeTypeImporterSelectors = importerSelectors(
  'placeType.importer',
);

export default placeTypeImporterSelectors;
