import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const reservationAgendaImporterSelectors = importerSelectors(
  'reservationAgenda.importer',
);

export default reservationAgendaImporterSelectors;
