import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const providersImporterSelectors = importerSelectors(
  'providers.importer',
);

export default providersImporterSelectors;
