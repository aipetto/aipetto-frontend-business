import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const messagesImporterSelectors = importerSelectors(
  'messages.importer',
);

export default messagesImporterSelectors;
