import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const languagesImporterSelectors = importerSelectors(
  'languages.importer',
);

export default languagesImporterSelectors;
