import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const questionsImporterSelectors = importerSelectors(
  'questions.importer',
);

export default questionsImporterSelectors;
